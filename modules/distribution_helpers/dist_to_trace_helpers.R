discrete_x <- function(type = c("d", "p", "q"), limits) {
  type <- match.arg(type)

  if (type %in% c("d", "p")) {

  } else {

  }
}

continuous_x <- function(type = c("d", "p", "q"), limits) {
  type <- match.arg(type)

  if (type %in% c("d", "p")) {

  } else {

  }
}
